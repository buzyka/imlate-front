Renders attendance state. Never invent labels or colours — signed_in is green "Signed in", signed_out is amber "Signed out", not_signed is grey "Not signed".

```jsx
<StatusTag status={row.sign_status} />
```
