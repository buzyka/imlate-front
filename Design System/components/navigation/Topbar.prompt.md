The app chrome above the content column. Only 36px tall — do not put filters, search or page titles in it; those belong in the page's first Card.

```jsx
<Topbar userName="Alex Morgan" onProfile={goProfile} onLogout={logout} />
```
