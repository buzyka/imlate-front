Card-level heading with a description and a single action.

```jsx
<PageHeader title="Settings" description="Manage tracking page branding and animation timings."
  actions={<Button loading={loading} onClick={refresh}>Refresh</Button>} />
<SectionTitle>Fire report</SectionTitle>
```
