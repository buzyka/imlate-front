Splits one card's body into named sections. Used inside Settings; keep it inside a Card, never as page-level navigation.

```jsx
<Tabs active={tab} onChange={setTab} tabs={[{name:'theme',label:'Theme',content:<ThemeGrid/>}]} />
```
