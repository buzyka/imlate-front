Boolean flags in forms (Is Student, Active). Never used for filtering — filters use Select.

```jsx
<FormField label="Is Student"><Switch checked={isStudent} onChange={setIsStudent} /></FormField>
```
