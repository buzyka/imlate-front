Long-form note entry. ImLate uses `rows={4}` and `resize="none"`, with an example-style placeholder.

```jsx
<Textarea value={reason} onChange={setReason} rows={4}
  placeholder="Example: Visitor arrived at reception and forgot to scan." />
```
